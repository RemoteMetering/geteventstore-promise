services:
  volumes-provisioner:
    image: hasnat/volumes-provisioner
    environment:
      PROVISION_DIRECTORIES: "1000:1000:0755:/tmp/certs"
    volumes:
      - certs_single:/tmp/certs
    network_mode: none

  metronomic_kurrentdb_client_test_setup_certs_single:
    image: eventstore/es-gencert-cli:1.0.2
    entrypoint: bash
    user: "1000:1000"
    command: >
      -c "mkdir -p ./certs && cd /certs
      && es-gencert-cli create-ca
      && es-gencert-cli create-node -out ./single -ip-addresses 127.0.0.1,172.30.239.11 -dns-names localhost
      && find . -type f -print0 | xargs -0 chmod 666"
    container_name: metronomic_kurrentdb_client_test_setup_certs_single
    volumes:
      - ./certs:/certs
    depends_on:
      - volumes-provisioner
    networks:
      singleclusternetwork:
        ipv4_address: 172.30.239.10

  metronomic_kurrentdb_client_test_single:
    image: kurrentplatform/kurrentdb:26.1
    container_name: metronomic_kurrentdb_client_test_single
    environment:
      - KURRENTDB_MEM_DB=true
      - KURRENTDB_CLUSTER_SIZE=1
      - KURRENTDB_RUN_PROJECTIONS=All
      - KURRENTDB_START_STANDARD_PROJECTIONS=true
      - KURRENTDB_REPLICATION_IP=172.30.239.11
      - KURRENTDB_ENABLE_ATOM_PUB_OVER_HTTP=true
      - KURRENTDB_DISCOVER_VIA_DNS=false
      - KURRENTDB_ADVERTISE_HOST_TO_CLIENT_AS=127.0.0.1
      - KURRENTDB_NODE_PORT_ADVERTISE_AS=22117
      - KURRENTDB_TRUSTED_ROOT_CERTIFICATES_PATH=/certs/ca
      - KURRENTDB_CERTIFICATE_FILE=/certs/single/node.crt
      - KURRENTDB_CERTIFICATE_PRIVATE_KEY_FILE=/certs/single/node.key
    ports:
      - "11116:1113"
      - "22117:2113"
    volumes:
      - ./certs:/certs
    depends_on:
      - metronomic_kurrentdb_client_test_setup_certs_single
    restart: always
    networks:
      singleclusternetwork:
        ipv4_address: 172.30.239.11

networks:
  singleclusternetwork:
    name: metronomic_kurrentdb_client_test_single_kurrentdb.local
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: 172.30.239.0/24

volumes:
  certs_single: